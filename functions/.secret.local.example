ROBOFLOW_API_KEY=your-roboflow-api-key

# Copy to `functions/.secret.local` for local emulator testing (gitignored).
# For deployed functions, set the real secret instead:
#   firebase functions:secrets:set ROBOFLOW_API_KEY
